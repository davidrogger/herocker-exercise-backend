on:
  pull_request:
    types: [opened, synchronize]

jobs:
  jest:
    runs-on: ubuntu-latest
    # needs: [eslint]
    steps:
        uses: actions/checkout@v2
      - name: Install dependêncies
        run: npm install
      - name: Starting tests
        run: npm test